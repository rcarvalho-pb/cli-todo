version: "2"

sql:
  - engine: "sqlite"
    queries: "./db/sqlc/queries.sql"
    schema: "./db/migrations/000001_create_tables.up.sql"
    gen:
      go:
        package: "db"
        out: "./pkg/db"
